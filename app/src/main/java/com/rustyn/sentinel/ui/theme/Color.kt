package com.rustyn.sentinel.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core Backgrounds ──
val DarkBackground = Color(0xFF0B0F19)
val DarkSurface = Color(0xFF151D30)
val DarkSurfaceVariant = Color(0xFF1A2340)
val DarkSurfaceElevated = Color(0xFF1E2A45)

// ── Primary Accents ──
val PrimarySky = Color(0xFF38BDF8)
val PrimarySkyDark = Color(0xFF0EA5E9)
val AccentIndigo = Color(0xFF818CF8)
val AccentIndigoDark = Color(0xFF6366F1)

// ── Status Colors ──
val BlockRed = Color(0xFFF87171)
val BlockRedDark = Color(0xFFEF4444)
val SuccessGreen = Color(0xFF34D399)
val SuccessGreenDark = Color(0xFF10B981)
val WarningAmber = Color(0xFFFBBF24)
val WarningAmberDark = Color(0xFFF59E0B)

// ── Text ──
val TextLight = Color(0xFFF8FAFC)
val TextMuted = Color(0xFF94A3B8)
val TextSubtle = Color(0xFF64748B)

// ── Borders & Overlays ──
val BorderSlate = Color(0xFF334155)
val BorderSubtle = Color(0xFF1E293B)
val GlassOverlay = Color(0xFF1E293B).copy(alpha = 0.4f)
val GlassOverlayLight = Color(0xFF38BDF8).copy(alpha = 0.06f)

// ── Gradient Terminals ──
val GradientCyanStart = Color(0xFF06B6D4)
val GradientCyanEnd = Color(0xFF6366F1)
val GradientWarmStart = Color(0xFFF59E0B)
val GradientWarmEnd = Color(0xFFEF4444)
val GradientGreenStart = Color(0xFF10B981)
val GradientGreenEnd = Color(0xFF06B6D4)

// ── Shimmer / Glow ──
val ShimmerHighlight = Color(0xFFFFFFFF).copy(alpha = 0.05f)
val GlowCyan = Color(0xFF38BDF8).copy(alpha = 0.12f)
val GlowIndigo = Color(0xFF818CF8).copy(alpha = 0.10f)
val GlowRed = Color(0xFFF87171).copy(alpha = 0.10f)

// ── Secondary Container ──
val SecondaryContainer = Color(0xFF1E2A45)
val OnSecondaryContainer = Color(0xFFE2E8F0)
