package com.rustyn.sentinel.ui.theme

import androidx.compose.ui.graphics.Color

val DarkBackground = Color(0xFF0B0F19) // Very dark premium blue-gray
val DarkSurface = Color(0xFF151D30)    // High-contrast slate card backdrop
val PrimarySky = Color(0xFF38BDF8)     // Neon sky-blue accent
val AccentIndigo = Color(0xFF818CF8)   // Indigo highlight
val BlockRed = Color(0xFFF87171)       // Bright error-red highlight
val TextLight = Color(0xFFF8FAFC)      // Crisp white
val TextMuted = Color(0xFF94A3B8)      // Muted slate gray
val BorderSlate = Color(0xFF334155)    // Card divider border color
val GlassOverlay = Color(0xFF1E293B).copy(alpha = 0.4f)
